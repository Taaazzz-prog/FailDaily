# Configuration pour environnement local (sans Docker complet)
DB_HOST=localhost
DB_PORT=3306
DB_USER=faildaily_user
DB_PASSWORD=your_password_here
DB_NAME=faildaily
NODE_ENV=development
PORT=3000
JWT_SECRET=your_jwt_secret_key_here

# === FRONTEND URL ===
# Utilisé pour construire les liens d'email (reset password)
APP_WEB_URL=http://localhost:8080

# === SMTP (Nodemailer) ===
# Configurez ces variables pour activer l'envoi d'emails
# Exemple: compte SMTP de test ou provider (SendGrid, Mailgun, etc.)
# OVH (courriel) — exemples
# SMTP_HOST=ssl0.ovh.net
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=contact@faildaily.com
# SMTP_PASS=motdepasse-ovh
# SMTP_FROM=FailDaily <contact@faildaily.com>

# Valeurs par défaut (à surcharger dans l'environnement)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=FailDaily <no-reply@faildaily.com>
