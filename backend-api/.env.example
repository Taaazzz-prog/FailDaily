# ==========================================
# 🛡️ CONFIGURATION RATE LIMITING AVANCÉE
# ==========================================

# Rate Limiting Global
RATE_LIMIT_WINDOW=15                    # Fenêtre en minutes (défaut: 15)
RATE_LIMIT_MAX_REQUESTS=5000           # Requêtes max par fenêtre (défaut: 5000)

# Protection DDoS (par minute)
DDOS_PROTECTION_WINDOW=1               # Fenêtre en minutes (défaut: 1)
DDOS_PROTECTION_MAX=200                # Requêtes max par IP/minute (défaut: 200)

# Protection Authentification (par 15 minutes)
AUTH_PROTECTION_WINDOW=15              # Fenêtre en minutes (défaut: 15)
AUTH_PROTECTION_MAX=10                 # Tentatives max par IP (défaut: 10)

# Protection Upload (par 5 minutes)
UPLOAD_PROTECTION_WINDOW=5             # Fenêtre en minutes (défaut: 5)
UPLOAD_PROTECTION_MAX=20               # Uploads max par IP (défaut: 20)

# Monitoring et Alertes
RATE_LIMIT_LOGGING=true                # Activer logging des rate limits
RATE_LIMIT_ALERTS=true                 # Activer alertes si seuil atteint
RATE_LIMIT_ALERT_THRESHOLD=80          # Seuil d'alerte en % (défaut: 80%)

# Configuration Base de Données
DB_HOST=localhost
DB_PORT=3306
DB_NAME=faildaily
DB_USER=faildaily_user
DB_PASSWORD=your_secure_password

# JWT Configuration
JWT_SECRET=your_super_secure_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=https://faildaily.com,https://www.faildaily.com

# Environnement
NODE_ENV=production
PORT=3000
