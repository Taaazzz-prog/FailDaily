[
  {
    "table_name": "badge_definitions",
    "columns": "id (text), name (text), description (text), icon (text), category (text), rarity (text), requirement_type (text), requirement_value (integer), created_at (timestamp with time zone)"
  },
  {
    "table_name": "badges",
    "columns": "id (uuid), user_id (uuid), badge_type (text), category (text), rarity (text), name (text), description (text), icon (text), unlocked_at (timestamp with time zone), created_at (timestamp with time zone)"
  },
  {
    "table_name": "comments",
    "columns": "id (uuid), fail_id (uuid), user_id (uuid), content (text), is_encouragement (boolean), created_at (timestamp with time zone), updated_at (timestamp with time zone)"
  },
  {
    "table_name": "fails",
    "columns": "id (uuid), user_id (uuid), title (text), description (text), category (text), image_url (text), reactions (jsonb), comments_count (integer), is_public (boolean), created_at (timestamp with time zone), updated_at (timestamp with time zone)"
  },
  {
    "table_name": "profiles",
    "columns": "id (uuid), username (text), email (text), display_name (text), avatar_url (text), bio (text), stats (jsonb), preferences (jsonb), created_at (timestamp with time zone), updated_at (timestamp with time zone), legal_consent (jsonb), age_verification (jsonb), email_confirmed (boolean), registration_completed (boolean)"
  },
  {
    "table_name": "reactions",
    "columns": "id (uuid), fail_id (uuid), user_id (uuid), reaction_type (text), created_at (timestamp with time zone)"
  },
  {
    "table_name": "user_badges",
    "columns": "id (uuid), user_id (uuid), badge_id (character varying), unlocked_at (timestamp with time zone), created_at (timestamp with time zone)"
  }
]